const OWNER_UID = "mHOKC2jtXcO7MvdQZNIuaSspLcD2";
const loginForm=document.getElementById('login-form'), authStatus=document.getElementById('auth-status'), publisher=document.getElementById('publisher'), logoutBtn=document.getElementById('logout'), postForm=document.getElementById('post-form'), postStatus=document.getElementById('post-status'), postList=document.getElementById('post-list');
loginForm.addEventListener('submit',async e=>{e.preventDefault();authStatus.textContent='Signing in…';try{const cred=await portfolioAuth.signInWithEmailAndPassword(document.getElementById('email').value.trim(),document.getElementById('password').value);if(cred.user.uid!==OWNER_UID){await portfolioAuth.signOut();throw new Error('This account is not authorized as the site owner.');}authStatus.textContent='Signed in successfully.';}catch(err){authStatus.textContent=err.message||'Sign-in failed.';}});
logoutBtn.addEventListener('click',()=>portfolioAuth.signOut());
portfolioAuth.onAuthStateChanged(user=>{const owner=!!user&&user.uid===OWNER_UID;publisher.classList.toggle('hidden',!owner);logoutBtn.classList.toggle('hidden',!owner);loginForm.classList.toggle('hidden',owner);if(user&&!owner){authStatus.textContent='This account is not authorized.';portfolioAuth.signOut();}if(owner){authStatus.textContent='Signed in as owner.';loadPosts();}});
postForm.addEventListener('submit',async e=>{e.preventDefault();const user=portfolioAuth.currentUser;if(!user||user.uid!==OWNER_UID){postStatus.textContent='Please sign in with the owner account.';return;}const title=document.getElementById('post-title').value.trim(),body=document.getElementById('post-body').value.trim(),category=document.getElementById('post-category').value;if(!title||!body)return;postStatus.textContent='Publishing…';try{await portfolioDb.collection('posts').add({title,body,category,published:true,createdAt:firebase.firestore.FieldValue.serverTimestamp(),authorUid:user.uid});postForm.reset();postStatus.textContent='Post published.';loadPosts();}catch(err){postStatus.textContent='Could not publish: '+err.message;}});
async function loadPosts(){postList.textContent='Loading…';try{const snap=await portfolioDb.collection('posts').orderBy('createdAt','desc').limit(50).get();postList.innerHTML='';if(snap.empty){postList.textContent='No posts yet.';return;}snap.forEach(doc=>{const data=doc.data(),row=document.createElement('div');row.className='post-row';const h=document.createElement('h3');h.textContent=data.title||'(untitled)';const p=document.createElement('p');p.className='small';p.textContent=(data.category||'')+' · '+(data.published?'Published':'Draft');const del=document.createElement('button');del.className='admin-btn secondary';del.textContent='Delete';del.addEventListener('click',async()=>{if(confirm('Delete this post?')){try{await portfolioDb.collection('posts').doc(doc.id).delete();loadPosts();}catch(e){postStatus.textContent=e.message;}}});row.append(h,p,del);postList.append(row);});}catch(err){postList.textContent='Unable to load posts: '+err.message;}}
